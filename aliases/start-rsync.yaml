---
name: "start_rsync"
pack: "umccr"
description: "Start the rsync of a runfolder."
action_ref: "umccr.sync2hpc"
ack:
  format: "Transferring data..."
  append_url: true
formats:
  - display: "sync <runfolder>"
    representation:
      - "sync {{runfolder}}(to {{dest_host}})?"
