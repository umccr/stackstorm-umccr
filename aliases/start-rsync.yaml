---
name: "start_rsync"
pack: "umccr"
description: "Start the rsync of a runfolder."
action_ref: "umccr.sync2hpc"
ack:
  format: "Let's get to work..."
  append_url: true
formats:
  - display: "move conversion output <runfolder>"
    representation:
      - "move conversion output( from)? {{source_path}}(to {{dest_path='/data/cephfs/punim0010/data/arteria_test'}} on {{dest_host=spartan.hpc.unimelb.edu.au}} as {{rsync_ssh_user=brainstorm}})?"
