---
name: "sync2nci"
pack: "umccr"
description: "Start the rsync of a runfolder."
action_ref: "umccr.sync2hpc"
ack:
  format: "Transferring data to NCI..."
  append_url: true
formats:
  - display: "sync <runfolder name> to NCI"
    representation:
      - "sync {{ runfolder_name }} to NCI"
