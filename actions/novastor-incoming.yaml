---
name: novastor_incoming
pack: umccr
description: "Run the incoming pipeline, from runfolder detection to bcl2fastq conversion."
enabled: false
runner_type: mistral-v2
entry_point: workflows/novastor-incoming.yaml
parameters:
  runfolder:
    description: "The full path of the runfolder to check for a valid sample sheet."
    required: true
    type: string
  runfolder_name:
    description: "The name (not full path) of the runfolder."
    required: true
    type: string
