---
name: samplesheet_check
pack: umccr
description: "Check the SampleSheet.csv of a runfolder, if we can handle it automatically."
enabled: false
runner_type: mistral-v2
entry_point: workflows/samplesheet-check.yaml
parameters:
  runfolder:
    description: "The full path of the runfolder to check for a valid sample sheet."
    required: true
    type: string
  runfolder_name:
    description: "The name (not full path) of the runfolder."
    required: true
    type: string
